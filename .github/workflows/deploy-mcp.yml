name: Deploy Image Processing MCP Service

on:
  workflow_call:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Generate image tag
        id: tag
        run: |
          TAG="$(date +%Y%m%d-%H%M%S)-${{ github.sha }}"
          echo "tag=${TAG}" >> $GITHUB_OUTPUT

      - name: Build and push Image Processing MCP service
        uses: docker/build-push-action@v5
        with:
          context: ./services/img-proc-mcp
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/img-proc-mcp:${{ steps.tag.outputs.tag }}
          cache-from: type=registry,ref=${{ secrets.DOCKERHUB_USERNAME }}/img-proc-mcp:buildcache
          cache-to: type=registry,ref=${{ secrets.DOCKERHUB_USERNAME }}/img-proc-mcp:buildcache,mode=max

      - name: Deploy to Dev
        if: github.ref == 'refs/heads/dev'
        uses: appleboy/ssh-action@v1
        with:
          host: dev.hadi.fursa.click
          username: ubuntu
          key: ${{ secrets.DEV_INSTANCE_SSH_KEY }}
          script: |
            cd /home/ubuntu/Poly-fursa-AI
            git fetch origin
            git checkout dev
            git pull --ff-only origin dev
            
            touch .env
            
            TAG="${{ steps.tag.outputs.tag }}"
            
            if grep -q '^MCP_IMAGE_TAG=' .env; then
              sed -i "s|^MCP_IMAGE_TAG=.*|MCP_IMAGE_TAG=${TAG}|" .env
            else
              echo "MCP_IMAGE_TAG=${TAG}" >> .env
            fi
            
            docker compose pull img-proc-mcp
            docker compose up -d --no-deps img-proc-mcp
            docker compose ps img-proc-mcp

      - name: Deploy to Prod
        if: github.ref == 'refs/heads/main'
        uses: appleboy/ssh-action@v1
        with:
          host: prod.hadi.fursa.click
          username: ubuntu
          key: ${{ secrets.PROD_INSTANCE_SSH_KEY }}
          script: |
            cd /home/ubuntu/Poly-fursa-AI
            git fetch origin
            git checkout main
            git pull --ff-only origin main
            
            touch .env
            
            TAG="${{ steps.tag.outputs.tag }}"
            
            if grep -q '^MCP_IMAGE_TAG=' .env; then
              sed -i "s|^MCP_IMAGE_TAG=.*|MCP_IMAGE_TAG=${TAG}|" .env
            else
              echo "MCP_IMAGE_TAG=${TAG}" >> .env
            fi
            
            docker compose pull img-proc-mcp
            docker compose up -d --no-deps img-proc-mcp
            docker compose ps img-proc-mcp
