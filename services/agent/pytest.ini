[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --cov=. --cov-report=xml --cov-report=html --cov-report=term-missing --tb=short
# Don't collect the app module itself, just tests
norecursedirs = __pycache__ .git .venv
