apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: app-of-apps
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/codgenius/Poly-fursa-AI.git
    targetRevision: main
    path: infra/k8s/argo
    directory:
      # Bootstrap applies this root Application directly. Excluding its own
      # manifest prevents the parent from recursively managing itself.
      exclude: app-of-apps.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: false
      selfHeal: false
    syncOptions:
      - CreateNamespace=true
