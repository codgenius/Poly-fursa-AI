apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: yolo
  namespace: dev
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: yolo
  namespaceSelector:
    matchNames:
    - dev
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
