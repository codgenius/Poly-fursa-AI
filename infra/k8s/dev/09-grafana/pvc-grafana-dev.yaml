apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana-pvc-dev
  namespace: dev
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ebs-sc
  resources:
    requests:
      storage: 2Gi