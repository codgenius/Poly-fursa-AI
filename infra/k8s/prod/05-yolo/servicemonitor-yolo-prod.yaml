apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: yolo
  namespace: prod
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: yolo
  namespaceSelector:
    matchNames:
    - prod
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
