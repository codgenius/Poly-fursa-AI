apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prometheus-pvc-prod
  namespace: prod
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ebs-sc
  resources:
    requests:
      storage: 5Gi